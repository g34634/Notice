<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.human.notice.repository.NoticeDAO">
    <select id="getNoticeList" resultType="com.human.notice.vo.NoticeVO">
        SELECT * FROM tb_notice WHERE n_status = 'N' ORDER BY n_regdate DESC
    </select>

    <insert id="insertNotice">
        INSERT INTO tb_notice (n_category, n_title, n_content)
        VALUES (#{n_category}, #{n_title}, #{n_content})
    </insert>
</mapper>
